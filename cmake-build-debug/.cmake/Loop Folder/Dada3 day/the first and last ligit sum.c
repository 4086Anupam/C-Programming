#include<stdio.h>

int main() {
    int num, last
    digit, fast
    digit, sum ;
    scanf("%d" & num);

   last digit= num % 10 ;
    while (num >= 10) {
        fast digit = n/10;
        sum = fast
        digit + last
        digit;
    }

    return 0;



}













