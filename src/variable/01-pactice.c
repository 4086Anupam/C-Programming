#include<stdio.h>
//WELCOME TO ANUPAM's IDE :)

int main() {
    int length, breadth;
    printf("Enter the length: ");
    scanf("%d", &length);
    printf("Enter the breadth: ");
    scanf("%d", &breadth);
    int asss = length * breadth;
    printf("the area of this reactangle is %d ", asss);
    return 0;
}