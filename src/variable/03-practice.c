#include<stdio.h>
//WELCOME TO ANUPAM's IDE :)

int main()
{
    int radius=4;
    float pi=3.14;
  printf("area of the circule %f\n",pi* radius*radius);
  int height=8;
  printf("the voluime of a cylinder is %f\n", pi*radius*radius*height);
    return 0;
}